# Owner's inbox (root Rule #18) — local only
UV/

# Generated ICO output — computed from SVG on demand (root Rule #19)
ico/

# Local-only sources & manifest (hidden projects) — never pushed
sources_local/
manifest.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Temp
*.tmp
.shortcuts.json

# Claude Code local settings — .claude/settings.json is TRACKED (project
# hook configuration, root CODE.md Enforcement); everything else under
# .claude/ (local overrides, session state) is not.
.claude/*
!.claude/settings.json

# Claude Code working folder - never tracked (owner decree 2026-08-09)
.claude/
